{
  "scripts": { 
    "lint": "(yarn run --silent lint)",
    "build:clean": "rm app/assets/webpack/*",
    "build:production": "(yarn run --silent build:production)",
    "build:production:client": "(yarn run --silent build:production:client)",
    "build:production:server": "(yarn run --silent build:production:server)",
    "build:client": "(yarn run --silent build:client)",
    "build:server": "(yarn run --silent build:server)",
    "build:dev:client": "(yarn run --silent build:dev:client)",
    "build:dev:server": "(yarn run --silent build:dev:server)",
    "hot-assets": "(yarn run hot-assets)",
    "start": "(yarn run --silent start)"
  },
  "main": "server-express.js",
  "engines": {
    "node": "^7.8.0 || ^8.0.0 || ^9.2.0 || ^10.0.0 || ^11.0.0 || ^11.13.0",
    "yarn": "1.12.3"
  },
  "name": "cambio-store",
  "private": true,
  "dependencies": {
    "@babel/preset-react": "^7.0.0",
    "@rails/webpacker": "^4.0.7",
    "babel-plugin-transform-react-remove-prop-types": "^0.4.24",
    "prop-types": "^15.7.2",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-on-rails": "11.3.0"
  },
  "devDependencies": {
    "webpack-dev-server": "^3.7.2"
  }, 
  "cacheDirectories": [
    "node_modules"
  ]
}
